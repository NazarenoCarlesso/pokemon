export default function validate(pokemon) {
    const { name, imagePokedex, imageDetail, types, height, // eslint-disable-line no-unused-vars
        weight, health, attack, defense, speed } = pokemon // eslint-disable-line no-unused-vars

    return 'ERROR EN VALIDATE'
}